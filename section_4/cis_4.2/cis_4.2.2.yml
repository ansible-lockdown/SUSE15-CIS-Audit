---

{{ if .Vars.suse15cis_level_1 }}
  {{ if .Vars.suse15cis_rule_4_2_2 }}
command:
  firewall_drops_unnecessary:
    title: 4.2.2 | Ensure firewalld drops unnecessary services and ports | Manual Check Required
    exec: echo "Manual check - review 'firewall-cmd --list-all' and confirm only approved services and ports are open"
    exit-status: 0
    stdout: []
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 4.2.2
      NIST800-53R5: CA-9
  {{ end }}
{{ end }}
